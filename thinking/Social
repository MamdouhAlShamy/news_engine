user will request users on the server
server will respond w
{users:
[ user_id:
	first_name:
	last_name:
	mobile_no:	

]
}

Client will ask to friend user w id user_id
server will create a row in table Follow
user_id(session_user)<FOLLOW>user_id

so when user(session_user) request to get news read by users he follow
from read table get story_id where users are user_id
>
Testing
Debug in the scheduler << DONE
* user can request to see news stories that are read by users he follows
	- Query userWhoIfollow << DONE
	- Query all read stories by these users << DONE
	- formate them in json << DONE
	& will I return the headline seprate then the read by users wala all as one package ? << mobile should do that
	- if the story read by the greatest number of users put first << DONE
	- remove dublicated story from diff users << DONE
	
* list of users that he follow


## Sharing
create a model of "Share" user_id, story_id <<DONE
create controller << DONE
hit story as shared << DONE
user can request to see news stories that are shared by users he follows << DONE
pirorty stories shared/read first then read
list of users I follow <<

Code Coverage


